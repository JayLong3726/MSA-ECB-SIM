const state={battery:100};function renderSlots(){const t=document.getElementById("slots");t.innerHTML="";for(let i=1;i<=6;i++){const r=document.createElement("div");r.className="slot";r.innerHTML=`<div class="slot-number">${i}</div><div class="slot-label">Slot Available</div>`;t.appendChild(r)}}function updateClock(){document.getElementById("clock").textContent=new Date().toLocaleTimeString([],{hour:"2-digit",minute:"2-digit",hour12:false})}renderSlots();updateClock();setInterval(updateClock,1000);